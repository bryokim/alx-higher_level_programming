``101-add_attribute`` module test file.
======================================

Using ``add_attribute`` function:
---------------------------------

Import function:
    >>> add_attribute = __import__('101-add_attribute').add_attribute

    >>> class NewClass:
    ...     pass

Tests:

    >>> nc = NewClass()

    >>> add_attribute(nc, "name", "Brian")
    >>> print(nc.name)
    Brian

    >>> add_attribute(nc, "name", "Kim")
    >>> print(nc.name)
    Kim

    >>> age = 21
    >>> add_attribute(age, "name", "Brian")
    Traceback (most recent call last):
        ...
    TypeError: can't add new attribute

    >>> name = "Kim"
    >>> add_attribute(name, "last_name", "Ndwiga")
    Traceback (most recent call last):
        ...
    TypeError: can't add new attribute
